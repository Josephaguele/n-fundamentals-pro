export const connection: Connection = {
  CONNECTION_STRING: 'MYSQL_DUMMY_CONNECTION_STRING',
  DB: 'MYSQL',
  DBNAME: 'TEST',
};
export type Connection = {
  CONNECTION_STRING: string;
  DB: string;
  DBNAME: string;
};
